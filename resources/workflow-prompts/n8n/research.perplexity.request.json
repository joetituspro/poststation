{
  "model": "perplexity/sonar-pro",
  "messages": [
    {
      "role": "system",
      "content": "You are a research assistant. Return only valid JSON that strictly follows the provided schema. Do not include markdown, explanations, or extra text."
    },
    {
      "role": "user",
      "content": "Your task is to find the top {{ $('Webhook').item.json.body.content_fields.body.sources_count || 3 }} most relevant, high-quality, currently ranking articles for the following topic or keyword.\n\nTopic/Keyword: {{$('Webhook').first().json.body.topic || ''}}\n\nResearch Requirements:\n- Only return real, publicly accessible URLs.\n- Only return article-based sources (e.g. blog posts, news articles, editorial pieces, research/study pages). Do NOT include homepages, category pages, tag pages, product pages, landing pages, or any URL that is not a standalone article.\n- Prioritize authoritative domains.\n- Prefer articles published recently unless evergreen.\n- Avoid forums unless highly authoritative.\n- Avoid duplicate domains unless unavoidable.\n- The articles must directly match the search intent of the topic.\n\nFor each article, extract:\n- Title\n- Full URL\n\nReturn ONLY valid JSON."
    }
  ],
  "response_format": {
    "type": "json_schema",
    "json_schema": {
      "schema": {
        "type": "object",
        "properties": {
          "topic": { "type": "string" },
          "articles": {
            "type": "array",
            "minItems": {{ $('Webhook').item.json.body.content_fields.body.sources_count }},
            "maxItems": {{ $('Webhook').item.json.body.content_fields.body.sources_count }},
            "items": {
              "type": "object",
              "properties": {
                "title": { "type": "string" },
                "url": { "type": "string" }
              },
              "required": [
                "title",
                "url"
              ]
            }
          }
        },
        "required": [
          "topic",
          "articles"
        ]
      }
    }
  }
}
